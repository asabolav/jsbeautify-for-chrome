export const DEFAULT_SETTINGS = {
  enabled: true,
  indentSize: 2,
  indentWithTabs: false,
  maxPreserveNewlines: 10,
  maxLineLength: 120,
  semicolonStyle: 'preserve', // preserve | add | remove
  braceStyle: 'collapse', // collapse | expand | end-expand | none
  spaceInEmptyParens: false,
  theme: 'dark'
};

export const MAX_AUTO_BYTES = 5 * 1024 * 1024;
